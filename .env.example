# Server-side AutoCount account book IDs. Set these in the server environment —
# they are secrets and must never reach the browser, HTML, or API responses.
# Each company key maps to exactly one account book; the two must be distinct.
AUTOCOUNT_ACCOUNT_BOOK_WANSON_ENTERPRISE=
AUTOCOUNT_ACCOUNT_BOOK_WANSON_SDN_BHD=
